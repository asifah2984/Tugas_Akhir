const kValidPasswordPattern = r'^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).+$';

const kDefaultErrorMessage =
    'Oops, unknown error occurred. Please try again later';

const kTotalSurah = 114;

const kTotalJuz = 30;
